{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd english-app/backend && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd english-app/backend && npm run build && npx prisma db push && node dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
