# Local development database. On Railway, set DATABASE_URL to
# "file:/data/english-app.db" after mounting a persistent volume at /data.
DATABASE_URL="file:./dev.db"
JWT_SECRET="CHANGE_THIS_TO_A_RANDOM_SECRET_STRING"
JWT_EXPIRATION="7d"
PORT=3000
NODE_ENV="production"
